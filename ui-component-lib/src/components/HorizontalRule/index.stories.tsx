import HoriztonalRule from './index';

const storybookData = {
  title: 'Components/HoriztonalRule',
  component: HoriztonalRule,
};

export default storybookData;

function Template() {
  return <HoriztonalRule />;
}

export const Default = Template.bind({});
