package normalizedstring

// this is a super simple wrapper around a string type that allows
// us to persist values to the database in a normalized way

// String is a DB wrapper type for a normalized string
type String string
